Zum Ausführen dieser Anwendung genügt Docker Compose. Einfach im Projektverzeichnis den Befehl `docker compose up -d` (oder `docker-compose up -d` bei älteren Installationen) ausführen — Docker Compose erstellt und startet alle benötigten Container, weitere lokale Konfiguration ist in der Regel nicht erforderlich.